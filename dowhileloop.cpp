#include <iostream>
#include "Burrito.h"

using namespace std;

int main(){
    int x = 1;

    do
    {
        cout << x <<endl;
        x++;
        /* code */
    } while (x<10);
    
    
    return 0;
}

// running code at least one time, that is the only reasion why people use the do whiele loop. This is the one of the most uncommon loop.
//You need to know how it works.
